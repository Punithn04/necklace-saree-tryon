# Copy to secrets.toml (git-ignored), or use the sidebar.
# Optional premium backend (Gemini AIza... or OpenAI sk-...):
# BYOK_KEY = "AIza..."
# Optional AI Horde key (free, speeds the queue):
# HORDE_API_KEY = "0000000000"
