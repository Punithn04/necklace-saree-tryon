# Copy to secrets.toml (git-ignored), or paste the key in the app sidebar.
# Blank / omitted = anonymous Horde access (slower). Free key: https://stablehorde.net/register
HORDE_API_KEY = "0000000000"
